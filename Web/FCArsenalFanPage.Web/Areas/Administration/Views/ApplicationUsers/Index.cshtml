@model IEnumerable<ApplicationUserViewModel>

@{
    ViewData["Title"] = "Products";
    
}

<h1 class="text-center mt-3 mb-4">Users Dashboard</h1>

<table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark text-center">
        <tr>
            <th>
              @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
               @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedOn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>

             <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody class="text-center">
        @foreach(var item in Model)
        {
             <tr>
            <td class="align-middle" style="width:25%">
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
                <td class="align-middle">
                @Html.DisplayFor(modelItem => item.Email)
            </td>
                <td class="align-middle">
                @Html.DisplayFor(modelItem => item.CreatedOn)
            </td>
                <td class="align-middle">
                    @Html.DisplayFor(modelItem => item.Role)
            </td class="align-middle">

            <td class="align-middle">
                    <a class="btn btn-primary mt-2 mb-2 ml-1" asp-controller="ApplicationUsers" asp-action="Edit" asp-route-id="@item.UserId">Edit</a>
                    <a class="btn btn-info mt-2 mb-2 ml-1"  asp-action="Details" asp-route-id="@item.UserId">Details</a>
                   <a class="btn btn-danger mt-2 mb-2 ml-1" asp-action="Delete" asp-route-id="@item.UserId">Delete</a> 
            </td>
        </tr>
        }
    </tbody>
</table>
